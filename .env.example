MONGODB_URI=mongodb+srv://<user>:<password>@ssayer-test.u3azo.mongodb.net/
MONGODB_DB_NAME=tax-monitor-test
PORT=3001
FRONTEND_URL=http://localhost:3000

# Shared interview DB: set true for read-only candidate users (trigger simulates ingest, no writes).
# Interviewer pre-seeds data with an admin account (see MongoMigration.md).
DB_READ_ONLY=true